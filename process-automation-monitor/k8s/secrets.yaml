apiVersion: v1
kind: Secret
metadata:
  name: process-monitor-secrets
  namespace: default
type: Opaque
stringData:
  postgres-password: "changeme"
  postgres-user: "orchestrator"
  mysql-password: "changeme"
  mysql-root-password: "changeme-root"
  mysql-analytics-password: "changeme"
  mysql-analytics-root-password: "changeme-root"
  jwt-secret: "change-this-secret-min-32-chars-replace-in-production"
  mail-password: "changeme"
# ─────────────────────────────────────────────────────────────────────────────
# IMPORTANT: This file contains placeholder values only.
# Before deploying to production:
#   1. Replace all values with strong, randomly generated secrets.
#   2. Manage secrets via an external secrets manager (e.g. AWS Secrets Manager,
#      HashiCorp Vault, Sealed Secrets) — never commit real credentials to git.
#   3. Rotate secrets regularly and restrict RBAC access to this Secret.
# ─────────────────────────────────────────────────────────────────────────────
